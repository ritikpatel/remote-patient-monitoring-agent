apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification-gateway.fullname" . }}
spec:
  selector:
    app: {{ include "notification-gateway.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
